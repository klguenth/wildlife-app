import React from 'react';
import './ResetButton.css';

export default function ResetButton() {
    return (
        <button type="reset" value="Reset" className="resetButton">
            Reset
        </button>
    );
}